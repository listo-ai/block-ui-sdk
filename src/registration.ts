/**
 * Block component registration helpers.
 *
 * Re-exported from `@listo/ui-core` so block authors have a single
 * import source for their registration calls.
 */
export {
  registerExtensionContributions,
  unregisterExtensionContributions,
  extensionRegistry,
} from "@listo/ui-core";
